# Local Video Uploader 技術説明書

## 1. アーキテクチャ

アプリは SwiftUI の macOS UI と Python 3.11+ CLI の 2 層構成である。Swift 側は画面状態、PhotoKit 操作、ローカル表示用データを担当し、Python 側は OAuth、YouTube Data API、メタデータ生成、SQLite、CSV を担当する。

```text
SwiftUI (ContentView / AppViewModel)
  ├─ PhotoLibraryService ── PhotoKit ── Photo Library
  ├─ HistoryCalendarRepository ── history_calendar.db
  ├─ MetadataHistoryStore ── metadata_history.json
  └─ CLIService ── .venv/bin/iphoto2youtube
                         ├─ OAuth / YouTube Data API
                         ├─ upload_history.db / management.db
                         └─ ledger.csv
```

ネイティブアプリは `NativeAppEnvironment` を通じ、作業ルート、CLI パス、サポートディレクトリを一元化する。既定では作業ルートの `.venv/bin/iphoto2youtube` と `./.iphoto2youtube` を使用する。`IPHOTO2YOUTUBE_WORKSPACE_ROOT` は作業ルート探索を上書きできる。

## 2. 主要ディレクトリと責務

| パス | 内容 |
| --- | --- |
| `Sources/iPhoto2YouTubeNativeApp/ContentView.swift` | SwiftUI レイアウト、画面、確認ダイアログ、履歴・照合 UI |
| `Sources/iPhoto2YouTubeNativeApp/AppViewModel.swift` | UI 状態、アップロードフロー、Photo Library ワークフロー、履歴カレンダー制御 |
| `Sources/iPhoto2YouTubeNativeApp/Models.swift` | UI モデル、SQLite / JSON のローカルストア、マニフェスト組立 |
| `Sources/iPhoto2YouTubeNativeApp/CLIService.swift` | Python CLI の起動、JSON 応答と進捗イベントの変換 |
| `Sources/iPhoto2YouTubeNativeApp/PhotoLibraryService.swift` | PhotoKit 認可、日付検索、キャッシュ書き出し、削除 |
| `src/iphoto2youtube_cli/cli.py` | `argparse` コマンドと JSON / table 出力 |
| `src/iphoto2youtube_cli/app.py` | ユースケース、バッチ制御、履歴・検証・同期のオーケストレーション |
| `src/iphoto2youtube_cli/services/` | Google OAuth と YouTube Data API アダプタ |
| `src/iphoto2youtube_cli/storage.py` | SQLite リポジトリ、CSV エクスポート、データ保持期限 |
| `src/iphoto2youtube_cli/metadata.py` | タイトル・説明・タグ・プレイリストの生成 |

## 3. Swift UI と状態管理

`AppViewModel` は `@MainActor` の単一状態管理点である。`CommonMetadata`、動画ドラフト、認証状態、進捗、照合結果、Photo Library 状態、履歴フィルタ、カレンダー状態を `@Published` で公開する。

`ContentView` は Upload、Recent History、History Calendar、Photos を表示する。破壊的操作とアップロードは `alert` で確認し、実行中は `isRunning` / `isPhotoLibraryBusy` により重複実行を防ぐ。

### CLI の起動と進捗

`CLIService` は `Process` で CLI を起動し、`--support-dir` を常に渡す。構造化結果は `--output json` で受け取り、JSON を UI モデルに変換する。CLI は stderr に `::progress::<JSON>` を出力でき、`CLIOutputCollector` が行単位で解析して `CLIProgressEvent` として UI へ渡す。通常の stderr はエラー表示用に保持する。

## 4. Python CLI

エントリポイントは `iphoto2youtube_cli.cli:main`、Python パッケージの実行は `python -m iphoto2youtube_cli` である。主要コマンドは以下の通りである。

| コマンド | 役割 |
| --- | --- |
| `auth-status` / `auth-login` / `auth-logout` | OAuth 状態・ログイン・トークン削除 |
| `current-channel` | 認証済みチャンネル取得 |
| `render-metadata` | API 呼び出しなしのメタデータ生成 |
| `upload` / `batch-upload` | 単体・JSON マニフェストによるアップロード |
| `verify-upload` | ローカルと YouTube 側のメタデータ・プレイリスト照合 |
| `sync-upload-metadata` | ローカル履歴に基づく YouTube 側の同期 |
| `history list/show`、`runs list/show` | 履歴・実行サマリーの参照 |
| `search-videos` | 管理 DB の属性・メディア情報検索 |
| `delete-local-history` / `delete-uploaded-video` | ローカルのみ、または YouTube とローカルの削除 |
| `backfill` | 過去履歴のメディア情報補完と CSV 再出力 |

`Application.initialize()` はサポートディレクトリと DB スキーマを準備し、期限切れの API 派生データを削除する。アップロード成功時は `upload_history.db` と `management.db` を更新し、`ledger.csv` を再出力する。重複判定は成功済み履歴を基に行う。

バッチ処理は項目ごとの失敗を結果に残して通常は継続するが、`quota`、`rate_limit`、`upload_limit`、`auth`、`permission` に分類される YouTube API エラーでは残りを未実行として終了する。

## 5. OAuth と YouTube Data API

`GoogleOAuthService` は `client_secret.json` と `token.json` を管理する。スコープは `youtube`、`youtube.force-ssl`、`youtube.upload` である。

`YouTubeUploadService` は動画アップロード、プレイリストの取得・作成・登録、動画削除、メタデータ同期を行う。アップロード後の検証では、YouTube 側の動画情報とプレイリスト所属を取得し、ローカルの期待値と比較する。プレイリスト満杯エラー時の代替先は `PlaylistRolloverRouteRepository` に保存する。

API 操作ごとの概算使用量は `UploadHistoryRepository` に記録し、日次ウィンドウの使用量、残量、操作別内訳を返す。これは UI 表示用のローカル推定であり、Google Cloud Console の公式クォータ残量そのものではない。

## 6. ローカルデータ

### Python 側

- `upload_history.db`: アップロード結果、実行ログ、クォータ計測、照合対象のローカル期待値。
- `management.db`: 検索可能な動画記録とプレイリストロールオーバー経路。
- `ledger.csv`: `management.db` のエクスポート。
- `config.json`: `AppSettings` が読み込むチャンネル制限、既定値、日次クォータ上限。

既定の保持期間は 30 日で、`UploadHistoryRepository.purge_expired_api_data()` と `VideoManagementRepository.purge_expired_api_data()` が起動時に処理する。

### Swift 側

`MetadataHistoryStore` は `metadata_history.json` に `places`、`eventNames`、`participantNames`、`playlists`、`cameraModels` と、対応する `suppressed...` 配列を保存する。`backfillFromUploadHistory()` は SQLite を優先し、必要時に CSV を参照する。`LedgerSuggestionLoader` は手動履歴・既存履歴・既定候補をマージし、抑止値を除外する。

`HistoryCalendarRepository` は `history_calendar.db` に日別カウント、手動マーク、メモ、Photo Library 削除イベントを保存する。`UploadLimitStateStore` は上限エラーから抽出または推定した再試行時刻を `upload_limit_state.json` に保存する。

## 7. Photo Library

`PhotoLibraryService` は PhotoKit の `.readWrite` 権限を使う。指定日の `PHAsset` を取得し、`mediaType == .video` のみを対象にするため、Live Photo の `pairedVideo` を誤ってアップロード対象にしない。

動画は `~/Library/Caches/iPhoto2YouTube/PhotoLibraryVideos/` へ書き出される。既にキャッシュされたアセットは再利用し、サムネイル PNG を生成する。キャッシュ削除はこのディレクトリだけを対象とする。Photo Library からの削除は `PHPhotoLibrary.performChanges` を経由する。

`AppViewModel.runPhotoLibraryWorkflow()` は、利用者が確認して開始した処理として、数字だけの `.mp4`、`VID_`、`HOVER_` をそれぞれ Vlog、Insta360、HoverX1 として順序どおり処理する。各段階の後にエラーと残存ドラフトを確認し、失敗時は次段階へ進まない。

## 8. ビルドとテスト

Python 依存関係は `pyproject.toml` で管理し、`pip install -e .` で `.venv` へ導入する。Swift Package は macOS 14 以上の executable target を定義する。

`script/build_and_run.sh` は `xcodebuild` で Debug ビルドし、`dist/Local Video Uploader.app` へ `ditto` でステージする。引数は `run`、`--debug`、`--logs`、`--telemetry`、`--verify`、`--build-only` を受け付ける。

確認には次を使用する。

```bash
python -m pytest
swift test
./script/build_and_run.sh --verify
```

`swift test` の実行環境によっては、ビルド後にテストランナー出力が待機したように見えることがある。その場合はビルド・リンクの成否と個別テスト出力を分けて確認する。
