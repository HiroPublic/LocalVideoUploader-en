# Local Video Uploader 仕様書

## 1. 目的と対象範囲

Local Video Uploader は、ローカル動画および macOS Photo Library の動画を、利用者本人が認証した YouTube チャンネルへ手動で登録・管理する macOS デスクトップアプリである。SwiftUI の画面と Python CLI を組み合わせ、YouTube Data API の操作とローカル履歴管理を提供する。

外部サーバー、定期実行、無人アップロードは使用しない。アップロード、YouTube 上のメタデータ同期、YouTube または Photo Library からの削除は、すべて利用者の明示操作と確認フローに基づく。

## 2. 対象環境・前提

- macOS 14 以降
- Python 3.11 以降と、リポジトリ内 `.venv/bin/iphoto2youtube` CLI
- YouTube Data API を有効にした Google OAuth デスクトップクライアント
- アップロード権限を持つ Google アカウント
- Photo Library を使う場合は、アプリへの写真ライブラリ読み書き権限

既定のサポートディレクトリは作業ルート直下の `./.iphoto2youtube` である。CLI 単体では `IPHOTO2YOUTUBE_HOME` または `--support-dir` で変更できる。

## 3. 機能要件

### 3.1 認証とチャンネル保護

- Google OAuth 2.0 でログイン、ログアウト、再認証できる。
- 認証済みのチャンネル ID、タイトル、ハンドルを表示する。
- `expected_channel` または `expected_channel_id` を設定した場合、一致しないチャンネルではログイン・アップロードを完了しない。
- OAuth パスワードは保存しない。トークンはローカルの `token.json` にのみ保存する。

### 3.2 動画キューとアップロード

- ローカルの `.mov` / `.mp4` を複数選択してキューへ追加できる。
- 各動画に撮影日時、内容、任意タイトル・説明、場所、イベント、参加者、カメラ、プレイリスト、メモを設定できる。
- Shared Metadata は新規ドラフトの初期値になり、**Apply Shared Values to Empty Fields** は空欄の項目だけを一括補完する。既存値は上書きしない。
- Dry Run は YouTube API を呼ばず、メタデータ生成とローカル DB / CSV 更新を検証する。
- 実アップロード前には宛先・件数・メタデータを確認するダイアログを表示する。
- 同一動画の成功済み履歴を検出した場合は、既定でスキップする。CLI の `--allow-duplicate` 指定時だけ続行できる。
- 進捗、各動画の結果、失敗理由を表示する。認証・権限・レート制限・クォータ・アップロード上限のエラーでは、残りのバッチを続行しない。

### 3.3 メタデータとプレイリスト

共通メタデータは Timezone、OffsetTimeOriginal、Location、Event、Participants、Camera、Playlist、Notes、Library Name、Capture Date Source、動画公開設定、プレイリスト公開設定で構成する。公開設定は `private`、`unlisted`、`public` から選択する。

- タイトル、説明、タグ、プレイリスト情報は入力値から生成する。個別タイトル・説明を指定した場合はそれを優先する。
- 存在しないプレイリストは指定した公開設定で作成する。
- YouTube がプレイリストの満杯を返した場合、ロールオーバー先を作成・記録し、以後の登録先として再利用する。

### 3.4 アップロード後の照合と修復

- アップロード完了後、YouTube 側のタイトル、説明、タグ、公開設定、チャンネル、処理状態、プレイリスト所属を取得してローカル記録と比較する。
- 差分は項目ごとに表示する。タグは不足分と余分を表示する。
- **Resolve Mismatch** は、ローカル履歴を正とし YouTube の動画メタデータとプレイリストを同期する。

### 3.5 メタデータ履歴

履歴対象は Location、Event、Participants、Playlist、Camera とする。

- 入力をコミットすると手動履歴として保存する。Participants はカンマまたは改行で分割して保存する。
- 起動時に `upload_history.db`、必要に応じて `ledger.csv` から候補をバックフィルする。
- 候補は新しい順に表示し、ポップオーバーから選択または削除できる。
- 削除した値は抑止情報として保存され、過去履歴に残っていても再表示しない。同じ値を再度入力・選択すると抑止を解除する。

### 3.6 Photo Library 連携

- 日付を指定して、その日に作成された動画のみを取得する。Live Photo のペア動画は対象外とする。
- 動画ファイルとサムネイルをローカルキャッシュへ書き出し、取得失敗件数を表示する。キャッシュ容量の確認と明示削除ができる。
- 選択した動画をアップロードキューへ追加できる。
- `VID_` は Insta360、`HOVER_` は HoverX1、数字だけの `.mp4` は Vlog としてプリセットできる。
- Photo Library ワークフローは、利用者が開始を選び確認した後、検出した Vlog → Insta360 → HoverX1 を順番にアップロードする。途中エラー時は直ちに停止する。Insta360 / HoverX1 の元動画は、そのアップロードが成功した後にのみ Photo Library から削除する。
- Photo Library の削除は選択動画に限定し、削除イベントを履歴カレンダーへ記録する。

### 3.7 履歴・検索・カレンダー

- Recent History ではアップロード履歴を表示し、キーワードまたは撮影日で絞り込める。
- 選択履歴を「ローカル履歴のみ」または「YouTube とローカル履歴」から削除でき、いずれも確認を要求する。
- CLI はタイトル、場所、イベント、カメラ、参加者、プレイリスト、動画時間、解像度、ファイルサイズで管理 DB を検索できる。
- History Calendar は日別のアップロード数、Photo Library 削除数、手動マーク、カテゴリ別調整、10 文字までのメモを保持する。再構築と更新ができる。

### 3.8 API クォータとデータ保持

- 日次 API 使用量、上限、残量、主要操作別内訳を表示する。使用量はローカル計測に基づく推定値を含む。
- アップロード上限エラーを検出した場合は、解除見込み時刻をローカルに保存・表示する。
- YouTube API 由来の履歴・管理データは最大 30 日保持し、起動時に期限切れデータを削除する。

## 4. 画面構成

左ペインには Connection、Authenticated Channel、YouTube Data API、Shared Metadata を配置する。右ペインは次の画面を切り替える。

- **Upload**: キュー編集、Dry Run、実アップロード、進捗、照合結果
- **Recent History**: アップロード履歴、検索、ローカル／リモート削除
- **History Calendar**: 月間集計、日別調整、メモ、Photo Library 削除履歴
- **Photos**: 日付別読み込み、キャッシュ管理、プリセット、Photo Library ワークフロー、Photo Library 削除

## 5. ローカル保存データ

- `config.json`: チャンネル保護、既定メタデータ、クォータ上限
- `client_secret.json` / `token.json`: OAuth クライアント設定とトークン
- `upload_history.db`: アップロード履歴、実行ログ、API 使用量
- `management.db`: 検索用動画情報、プレイリストロールオーバー経路
- `ledger.csv`: 管理動画の CSV エクスポート
- `history_calendar.db`: カレンダー集計、手動調整、メモ、削除イベント
- `metadata_history.json`: 手動履歴と削除抑止リスト
- `upload_limit_state.json`: 上限解除見込み
- `~/Library/Caches/iPhoto2YouTube/PhotoLibraryVideos/`: Photo Library からの書き出しファイルとサムネイル

## 6. 非機能・安全要件

- 外部サーバーへアプリ独自のデータを送信しない。YouTube API 通信は利用者の OAuth 認可の範囲に限る。
- 背景処理、スケジュール実行、無人アップロードを行わない。
- 破壊的な操作には対象を明示し、画面上で確認を求める。
- ソース変更を macOS アプリへ反映するには再ビルドと再起動が必要である。
